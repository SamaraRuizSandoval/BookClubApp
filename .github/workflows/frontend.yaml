name: Frontend - Build & Deploy (SWA CLI)

on:
  push:
    branches: [ main ]
    paths:
      - 'book-club-app/**'
      - '.github/workflows/frontend-merge-deploy.yml'
  workflow_dispatch:

concurrency:
  group: frontend-swa-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      # Vite: baked at build time; read with import.meta.env.VITE_API_BASE_URL
      VITE_API_BASE_URL: https://api.yourdomain.com

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: book-club-app/package-lock.json

      - name: Install deps
        run: npm ci
        working-directory: book-club-app

      - name: Build (Vite -> ./dist)
        run: npm run build
        working-directory: book-club-app

      - name: Ensure AZURE_SWA_TOKEN is set
        run: |
          if [ -z "${{ secrets.AZURE_SWA_TOKEN }}" ]; then
            echo "::error::AZURE_SWA_TOKEN is not set. In your SWA resource: Overview → Manage deployment token → add as repo secret.";
            exit 1
          fi

      - name: Deploy with SWA CLI
        run: |
          npm i -g @azure/static-web-apps-cli
          swa deploy ./dist \
            --app-name "bookclub-swa" \
            --resource-group "bookclub-rg" \
            --env production \
            --deployment-token "${{ secrets.AZURE_SWA_TOKEN }}" \
            --verbose=info
        working-directory: book-club-app
